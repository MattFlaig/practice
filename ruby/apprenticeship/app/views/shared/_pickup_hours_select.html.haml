= f.select :requested_delivery_at do
  = content_tag(:option, 'So schnell wie möglich', value: nil) unless hour_type == 'no_order'
  = delivery_values.each_with_index do |val, index|
    - if val.hour >= 18 && val.strftime('%H:%M') != '18:00' && val != delivery_values.last
      %option{ value: delivery_values[index], selected: val == delivery_values[index] }
        #{val.strftime('%H:%M')} - #{delivery_values[index + 1].strftime('%H:%M')} Uhr
